name: mvn test with JDK
on:
  push:
    paths:
      - '**'
jobs:
  os_matrix:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
      - name: checkout main repo
        uses: actions/checkout@v5

      - name: set up JDK 26
        uses: actions/setup-java@v5
        with:
          java-version: '26'
          distribution: 'zulu'
          cache: 'maven'

      - name: checkout tensor
        uses: actions/checkout@v5
        with:
          repository: datahaki/tensor
          path: tensor
      - name: build and install tensor
        run: mvn -B --no-transfer-progress install -DskipTests
        working-directory: ./tensor

      - name: checkout subare
        uses: actions/checkout@v5
        with:
          repository: datahaki/subare
          path: subare
      - name: build and install subare
        run: mvn -B --no-transfer-progress install -DskipTests
        working-directory: ./subare

      - name: checkout bridge
        uses: actions/checkout@v5
        with:
          repository: datahaki/bridge
          path: bridge
      - name: build and install bridge
        run: mvn -B --no-transfer-progress install -DskipTests
        working-directory: ./bridge

      - name: mvn test
        shell: bash
        run: |
          if [ "$RUNNER_OS" == "Linux" ]; then
            xvfb-run --auto-servernum mvn -B -Dstyle.color=always test
          else
            mvn -B -Dstyle.color=always test
          fi
